services:
  app:
    build: .
    ports:
      - "3002:3000"
    volumes:
      # コードの変更をリアルタイムで反映
      - .:/app
      # node_modulesは除外（コンテナ内のものを使用）
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
    command: npm run dev
